#include <iostream>
#include <memory>

// Removing the last element takes only constant because "no resizing happens".
// Inserting at the end takes differential time, as "sometimes there may be a need of extending the array".
// Inserting and erasing at "the beginning" or "in the middle" is linear in time.

// Resource: https://www.geeksforgeeks.org/how-to-implement-our-own-vector-class-in-c/

namespace my {
	
	template< class T > // typename or class
	class vec {
		public:
				
	};
};
